export interface Activity {
    id: string  //This is why in ActivityDashboard id is declared as a string instead of an int.
    title: string
    date: string
    description: string
    category: string
    city: string
    venue: string
  }